require "spec_helper"

RSpec.describe PavlovicViewTool do
  it "has a version number" do
    expect(PavlovicViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
